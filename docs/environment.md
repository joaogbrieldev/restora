# Variáveis de ambiente (Fase 1)

Quando Prisma e auth forem integrados, documentar em `.env.example`. Esperado:

- `DATABASE_URL` — conexão PostgreSQL
- `JWT_SECRET` — assinatura de tokens
- `PORT` — porta da API (opcional; padrão `3001`)
